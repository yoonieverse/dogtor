import { useState } from 'react';
import { Link } from 'react-router-dom';

function PrescreeningPage() {
  const [logoError, setLogoError] = useState(false);

  return (
    <div>
      <h1>Prescreening Page</h1>
    </div>
  );
}

export default PrescreeningPage;
